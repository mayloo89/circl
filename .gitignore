# Node / Next
node_modules
.npm
.pnpm-store
.yarn
.yarn/cache
.next
out
dist
coverage
*.log

# Env / secrets
.env
.env.*
!.env.example
!.env.prod.example

# Go
bin/
vendor/
*.exe
*.test
*.out
*.coverprofile
*.pprof
backend/api
backend/data/

# OS / IDE
.DS_Store
.idea/
.vscode/

# AI agent configs (local, not project-specific)
.claude/
.agents/
.opencode/
.impeccable/
skills-lock.json
CLAUDE.md
CLAUDE.local.md
PRODUCT.md

# Local CI script
run-ci-local.sh

# Operator-specific deploy config (each maintainer keeps their own copy:
# their domain, their nginx specifics, their secrets). The committed
# generic templates live in docs/deploy/. The leading slash anchors this
# to the repo root so docs/deploy/ is NOT excluded.
/deploy/
.env.prod
